# ポータル（CoreFlow）引き継ぎメモ（v0.3.0 / 2026-05-25）

> 🎯 **このアプリの位置付け**：小林モータース業務アプリ群（〜Flow）の**ホールディングス入口＝CoreFlow**。
> ①エコシステム型TOP（アプリランチャー）＋②**メンバー＆権限の一元管理（全アプリの「単一の管理元」）**。
> **名称＝CoreFlow（2026-05-25 確定。旧仮名「小林モータース ポータル」）**。CoreFlow＝各〜Flowアプリを束ねる中核プラットフォームの総称で、ポータルはその入口（顔）。将来はここを正式な入口に育て、ビジュアルを大改修する前提。

> 🆕 **2026-05-25（v0.3.0）＝ CoreFlow に改称**：タイトル／ログイン／TOP中央／フッター／戻るリンクを「CoreFlow」に。中心＋周回ドットのロゴSVG（ログイン・TOP両方）の**周回4ドットをアプリ色に色付け**（上=青CarFlow / 右=紫StockFlow / 下=緑PitFlow / 左=金）＝「白い核(CoreFlow)の周りをアプリが回る」意味。ワードマークは `Core`(白太)＋`Flow`(グレー)。サブタイトルは「KOBAYASHI MOTORS」。index.html のみ・GitHub Pages なので push だけ。最終ロゴ・ビジュアル大改修は今後（⑤）。

> 🔗 **共通ルールは `D:\アプリ開発\開発全体メモ.md` を参照**（特に §🏢共通基盤構想・§🆕2026-05-25 ポータル設計）。ここにはポータル固有の話だけ書く。

---

## 🌐 環境・URL

| 項目 | 内容 |
|---|---|
| GitHub | https://github.com/yuta19kmail-coder/kobayashi-portal |
| ローカル | `D:\アプリ開発\ポータル\` |
| 本番URL | https://yuta19kmail-coder.github.io/kobayashi-portal/ （GitHub Pages・main / root） |
| DB / 認証 | CarFlow と同じ Firebase **carflow-9d500** に相乗り（Google認証＋Firestore） |
| ホスティング | GitHub Pages（main に push すると自動再デプロイ） |

> ⚠️ Authentication 承認済みドメインに `yuta19kmail-coder.github.io` が入っていること（StockFlow と共通・追加済み）。

---

## 🗂 ファイル構成

```
ポータル/
├── index.html        ← アプリ本体（1ファイルにHTML/CSS/JS全部入り）
├── firebase-init.js  ← Firebase初期化（carflow-9d500 に相乗り・window.fb 公開）
└── top-mock.html     ← エコシステムTOPの当初デザインモック（叩き台・本番では未使用）
```

- バージョン表記：index.html 内（コミットメッセージは `vX.Y.Z`）。
- 動作確認は Ctrl+Shift+R（強制リロード）。

---

## 🟢 現在の進捗（v0.2.0）

- **ログイン**：本物のGoogleログイン（`loginWithGoogle`）。
- **入室ゲート**（`checkAccess`）：**ポータル管理に入れるのは CarFlow の admin/manager**（`companies/kobayashi_motors/staff` のロールで判定）。それ以外は弾く。
- **エコシステム型TOP**：中央ロゴ＋周回アプリアイコン（`APPS` 配列）。ホバーでそのアプリのテーマ色が広がる（`floodFrom`/`floodOff`）、クリックで各アプリへ（`enterApp`）。未提供アプリは「Coming Soon」。
- **メンバー名簿の実データ保存**（Firestore `portalMembers`）：一覧（`renderTable`）＋編集（`renderEditor`）。**役職・所属部署・各アプリの入室可否・管理者フラグ**を編集して保存（`saveMember`/`saveCurrent`）、追加（`addMember`）・削除（`delCurrent`/`deleteMember`）。**アイコンはCarFlow式の円形クロッパー**（`openCrop`/`applyCrop`、dataURL JPEGで保存）。
- **マスター管理者＝ゆうた（固定）**：`makeMasterDoc`/`ensureRoster` で名簿が空なら自動シード。マスターは**外せない・編集ロック**（権限を奪えない安全装置）。

### データモデル（Firestore）
- **名簿**：`companies/kobayashi_motors/portalMembers/{memberId}`
  - `memberId` は **uid** か、招待時の生成ID。各アプリは「uidで取得 → 無ければ email一致で検索」の順で自分を探す。
  - 主なフィールド：`name`（表示名）／`email`／`role`（役職）／`dept`（所属部署）／`active`（在籍）／`admin`（ポータル管理に入れるか）／`photo`（アバターdataURL）／**各アプリの入室可否**（例：`stockflow:{ on:true }`、今後 `carflow`/`pitflow` も同形）。
- **読み取りの正**：各アプリは**この名簿を読むだけ**。例＝StockFlow の入室判定＝`m.active!==false && m.stockflow.on===true`。

### firestore.rules（carflow側・デプロイ済み）
- `portalMembers`：**読み取り**＝admin/manager（全件）または本人（uid一致 or email一致で自分のレコードのみ）。**書き込み**＝admin/manager のみ。
- ※ルールは `D:\アプリ開発\CarFlow\carflow\firestore.rules`（CarFlowリポジトリ）にあり、`firebase deploy --only firestore:rules` でデプロイ。**本番ルール変更は🟡ゆうた確認のうえデプロイ**。

---

## 🎯 今後の予定（開発全体メモ §作る順 と対応）

- ✅ ①中央メンバー管理（ゆうた専用・実保存）＝**完了**
- ✅ ②StockFlow の入室を名簿に連動＝**完了**
- ⬜ ③PitFlow を名簿に追加（入室・役割・権限）
- ⬜ ④**本番CarFlow を慎重に後から合流**（CarFlow側のメンバー編集を「閲覧のみ」に。非破壊で後日）
- ⬜ ⑤ポータル本格化（最終ロゴ・ビジュアル大改修・ランチャー演出の作り込み）

---

## 🛠 デプロイコマンド（GitHub Pages なので push だけ）

```
cd D:\アプリ開発\ポータル
git add -A
git commit -m "vX.Y.Z: 〜〜〜（簡潔に1行）"
git push
```

> ⚠️ 過去にハマった点：リモートが `portal.git` になっていて push 失敗 → `git remote set-url origin https://github.com/yuta19kmail-coder/kobayashi-portal.git` で修正済み。`Repository not found` が出たら remote URL を確認する。

---

## ⚠️ 注意点

- **メンバー・権限の編集はここ（ポータル）が唯一の編集元**。各アプリ側に編集UIを作らない（作っても閲覧のみ）。アプリ固有のメンバー項目は「そのアプリの欄」の中で編集する。
- **マスター管理者（ゆうた）は外せない・編集ロック**を必ず維持する。
- 見た目はダーク優先・作り込み最小（後で大改修前提）。凝りすぎない。
- ルール変更・名簿の一括操作は本番CarFlowと同じ Firebase を触るので、🟡確認の上で。

---

## 📌 このメモの更新ルール
- バージョンを上げたら `🟢 現在の進捗` を更新
- 名簿のデータ構造（portalMembers のフィールド）を変えたら必ずここを更新
- アプリを名簿に追加したら `今後の予定` を更新
